/**
 * 1. Hàm
 * - Một khối mã
 * - Làm 1 việc cụ thể
 * 
 * 2. Loại hàm
 * - Build-in
 * - Tự định nghĩa
 * 
 * 3. Tính chất
 * - Không thực thi khi định nghĩa
 * - Thực thi khi gọi
 * - Có thể nhận tham số
 * - Có  thể trả về giá trị
 */

// Tên hàm có thể có a-z, A-Z, 0-9(không đặt ở đầu tên hàm), _, $

function showDialog() {
    alert('Hello')
} 

// call ()
showDialog()