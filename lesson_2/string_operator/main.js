/**
 * Toán tử chuỗi - String operator
 */

var firstName = 'Kien';
var lastName = 'Vu';

console.log(firstName + ' ' + lastName); // Kien Vu -> + là toán tử số học khi cả 2 vế là số, nhưng chỉ cần 1 trong 2 vế là chuỗi thì + trở thành toán tử nối chuỗi